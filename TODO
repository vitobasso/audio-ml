 - save trained weights to use later
 - load other sounds and try to apply on them
 - recurrent network: unsupervised training on sequence
 - link inputs directly to outputs then use less hidden units
 - normalize input
 - raw signal as input
